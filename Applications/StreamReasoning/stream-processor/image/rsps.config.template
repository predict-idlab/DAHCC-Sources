[rspservice]
host ? localhost
port ? $HOMELAB_RSP_ENGINE_PORT

[streams]
homelab ? $HOMELAB_STREAM

[queries]

[observers]
$CENTRAL_SERVER_EXTERNAL_PROTOCOL://$CENTRAL_SERVER_CENTRAL_REASONER_HOST_EXTERNAL:$CENTRAL_SERVER_CENTRAL_REASONER_PORT_EXTERNAL$CENTRAL_SERVER_CENTRAL_REASONER_URI/stream ? all
